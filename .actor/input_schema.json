{
    "title": "Avvo Lawyers Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "title": "Start URL",
            "type": "string",
            "description": "Direct Avvo lawyer directory URL to scrape from (e.g., https://www.avvo.com/bankruptcy-debt-lawyer/al.html).",
            "editor": "textfield",
            "prefill": "https://www.avvo.com/bankruptcy-debt-lawyer/al.html"
        },
        "maxLawyers": {
            "title": "Maximum Lawyers",
            "type": "integer",
            "description": "Max profiles to scrape (0 = unlimited — crawl to natural listing end).",
            "default": 0,
            "prefill": 0,
            "minimum": 0,
            "maximum": 100000
        },
        "maxListingPages": {
            "title": "Maximum Listing Pages",
            "type": "integer",
            "description": "Max listing pages to crawl (0 = unlimited — paginate until no new profiles).",
            "default": 0,
            "prefill": 0,
            "minimum": 0
        },
        "includeReviews": {
            "title": "Include Reviews",
            "type": "boolean",
            "description": "Fetch additional review-related details from profile pages. Enabling this will visit individual profile pages and may slow down runs.",
            "default": false,
            "prefill": false
        },
        "includeContactInfo": {
            "title": "Include Contact Information",
            "type": "boolean",
            "description": "Fetch additional contact details from profile pages (email, phone, office address). Enabling this will visit individual profile pages and may slow down runs.",
            "default": false,
            "prefill": false
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings for bypassing anti-bot protection. Highly recommended for reliable scraping.",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true
            },
            "prefill": {
                "useApifyProxy": true
            }
        }
    },
    "required": [
        "startUrl"
    ]
}
